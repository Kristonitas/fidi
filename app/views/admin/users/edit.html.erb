<hr>
  <%= form_for @user, url: admin_user_path(@user) do |form| %>
    <div class="field">
      <%= form.label :name %><br>
      <%= form.text_field :name %>
    </div>

    <div class="field">
      <%= form.label :is_fidi %><br>
      <%= form.check_box :is_fidi %>
    </div>

    <div class="field">
      <%= form.label :booth_id %><br>
      <%= form.number_field :booth_id %>
    </div>
    
    <%= form.submit 'Update' %>
  <% end %>
</hr>